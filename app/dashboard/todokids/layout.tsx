/* ============================================================
   TodoKids — Layout override
   Contourne le DashboardShell (navigation staff) pour offrir
   une expérience plein écran adaptée aux enfants.
   ============================================================ */
export default function TodoKidsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
